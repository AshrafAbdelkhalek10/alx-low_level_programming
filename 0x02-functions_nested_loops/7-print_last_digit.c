#include "main.h"
/**
 *print_last_digit - to print the last digit
 *@a: The parameter to be checked
 * Return: 1,0 (Success)
 */
int print_last_digit(int a)
{
	int rem;

	rem = a % 10;
	return (rem);
}
